import { ServantListFilterPipePipe } from './servant-list-filter-pipe.pipe';

describe('ServantListFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ServantListFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
